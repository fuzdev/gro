---
'@fuzdev/gro': minor
---

**breaking** chore: the peer dep `@fuzdev/blake3_wasm` is renamed to `@fuzdev/blake3-wasm` (`^0.2.0`), and the `@fuzdev/fuz_util` peer floor rises to `>=0.69.0` (the release that imports the new name)
